class TiposDeDatos {
  public static void main(String args[]){
    byte size = 50l
    short superficieHabitable = 1203;
    // Adding [L] at end because a literal of a integer number is a int.
    /**
     * En Java, el valor por defecto de un literal numérico como 12313 
     * sería de tipo int. Esto se debe a que, por defecto, los números enteros sin un sufijo específico (como L para long o f para float) se consideran de tipo int.
     * Si deseas que el valor sea de otro tipo, como long, deberías agregar el sufijo L al número, como en 12313L.
     * 
     */
    long distancia = 5056050560L;
    boolean isActive = false;
    // Adding [f] at end because a literal of a decimal number is a double.
    float sueldo = 1000.015f;
    double superficie = 10000000.25540;



  }  
}